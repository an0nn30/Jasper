/**
 * Immutable terminal appearance and launch geometry values. Builders validate configuration before it reaches a live view or process.
 * <p>Depends only on JDK values; immutable instances are shareable, builders are thread-confined. TerminalOptions, Palette and GridSize define validation and defaults.
 */
package dev.jasper.terminal.config;
