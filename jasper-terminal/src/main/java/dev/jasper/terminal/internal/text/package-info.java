/**
 * Unsupported Jasper-owned cell, row, selection and text algorithms. Absolute row equals discarded lines plus history lines plus screen row.
 * <p>Depends only on config and JDK types. Algorithms have no Swing or vendor dependency. TerminalRow live adapters require the buffer lock; detached captures can be read afterward.
 */
package dev.jasper.terminal.internal.text;
