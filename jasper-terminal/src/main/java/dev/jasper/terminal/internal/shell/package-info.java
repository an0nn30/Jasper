/**
 * Unsupported shell protocol filtering, prompt marks, and command tracking. Shell positions use absolute rows.
 * <p>ShellIntegrationFilter rewrites streaming protocol; ShellCommandTracker interprets ordered events on the reader. Dependencies are JDK and text values; never Swing or facades.
 */
package dev.jasper.terminal.internal.shell;
