/**
 * Supported font selection and cell metrics for terminal embedding.
 * <p>Depends only on AWT/JDK types. FontSet caches glyph choices and must remain confined to its owning EDT.
 */
package dev.jasper.terminal.rendering;
