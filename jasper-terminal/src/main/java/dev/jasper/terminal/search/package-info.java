/**
 * Vendor-free search requests and immutable match-count/navigation results. Internal match coordinates use absolute rows and are not exposed by FindResult.
 * <p>Depends only on JDK values. SearchQuery and FindResult are immutable and safe to pass between worker and EDT.
 */
package dev.jasper.terminal.search;
