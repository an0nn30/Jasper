// Run from the repository root: ./gradlew :moray-app:mockUiPreview --init-script docs/design/plan-4c-themes/preview.gradle
allprojects {
    afterEvaluate {
        if (name == 'moray-app') {
            tasks.named('mockUiPreview') {
                mainClass = 'dev.moray.app.SystemThemePreview'
                setArgs([rootProject.file('docs/design/plan-4c-themes').absolutePath])
            }
        }
    }
}
