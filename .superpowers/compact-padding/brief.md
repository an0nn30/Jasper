# Bounded follow-up: copyable example and compact terminal padding
Worktree /Users/dustin/projects/moray/.worktrees/compact-padding; base65e063a. User supplied Moray/iTerm screenshots and explicitly wants smaller terminal edge padding and a repo example config to install. Root approved routine4logical-pixel inset on every side to approximate iTerm, replacing24. This supersedes previous mock padding.

Implement only:
1. TerminalPane padding4px all sides, use one package-private static padding constant consumed by InitialWindowSize (total8eachdimension). Preserve toolbar/title/statusgeometry, appfont, terminalrendercoordinatepaths, session lifecycle. No new padding config setting required.
2. Root config.example.toml: actual copyable UTF8 TOML with every Plan4b supported non-shortcut setting as explicit built-in defaults, empty terminal.env plus commented example, keybindings header with optional commented platform-specific examples (no active crossplatform ambiguous bindings). Comments for ranges/options/live vs new pane/window, exact shell args/environment. Use current ConfigTemplate/docs/configuration as authority. Do not overwrite real user config.
3. Docs: README prominent link to root example and macOS install command using mkdir -p ~/.config/moray and cp -i config.example.toml ~/.config/moray/config.toml fromrepo/worktree root; explain prompt if existing. config guide links example and4px geometry. STATUS short followup and native check:4pxsupersedesold24. Mark old mock spec/manual and Plan4b geometry spec/plan notes superseded with concise banners/clarification; preserve historical measured artifacts (do not rewrite oldimages/measurements). No elaborate new spec/plan.
4. Focused tests: adjust existing InitialWindowSize expectations to correct8total; meaningful actual pane layout inset/sizing regression across theme changes, parser parses actual on-disk example with no diagnostics and complete defaults in bothplatformmodes. Do not source-string-mirror implementation; no elaborate generator/build task. Tests may read root example using Gradle rootproject-based test input property if needed. Ensure modified file inputs rerunthat test. Existing ConfigTemplate already supportsnewoptions, do not restructureit.

Follow TDD focused RED/GREEN forpadding/layout andactualexampleparse, then full ./gradlew check, exactXML counts, sourcehygiene/diffcheck. Existing442total441pass1knownfontskip baseline. No GUI/login shells/editor/benchmark/actualaudio, no userconfigwrites, agents/reviewers, merge/push. Read AGENTS.md. Commit task files with Co-Authored-By trailer. No unrelated hardening/refactor. Root handles independent review and actualheadlesspreview inspection after implementation. Send a concise milestone when implementationandfocusedchecksareavailable. Report fullcommands/results/APIchanges/concerns in .superpowers/compact-padding/report.md; finalshort status/commit/tests/concerns.
