/**
 * Stable launcher only; delegates to bootstrap. No owned resources.
 * <p>Allowed outgoing Jasper dependencies: dev.jasper.app.bootstrap.
 * The module architecture check forbids package cycles; app types are not an external plugin API.
 */
package dev.jasper.app;
